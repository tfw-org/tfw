package tfw.tsm;

/**
 * An interface which defines a container of a {@link Branch}.
 */
public interface BranchBox {
    /**
     * Returns the branch associated with this box.
     * @return the branch associated with this box.
     */
    public Branch getBranch();
}
