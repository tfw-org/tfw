package tfw.stream.bitis;

/**
 * This interface defines a factory that creates BitIs objects.
 */
public interface BitIsFactory {
	/**
	 * Create a BitIs object.
	 * 
	 * @return the BitIs object.
	 */
	BitIs create();
}
