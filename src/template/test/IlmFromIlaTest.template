package tfw.immutable.ilm.%%PACKAGE%%ilm.test;

import java.util.Random;
import junit.framework.TestCase;
import tfw.immutable.ila.%%PACKAGE%%ila.%%NAME%%Ila;
import tfw.immutable.ila.%%PACKAGE%%ila.%%NAME%%IlaFromArray;
import tfw.immutable.ilm.%%PACKAGE%%ilm.%%NAME%%Ilm;
import tfw.immutable.ilm.%%PACKAGE%%ilm.%%NAME%%IlmFromArray;
import tfw.immutable.ilm.%%PACKAGE%%ilm.%%NAME%%IlmFrom%%NAME%%Ila;

public class %%NAME%%IlmFrom%%NAME%%IlaTest extends TestCase
{
	public void test%%NAME%%IlaFrom%%NAME%%Ila()
	{
		final Random random = new Random();
		final int WIDTH = 10;
		final int HEIGHT = 9;
	
		%%TYPE%%[] ilaArray = new %%TYPE%%[WIDTH*HEIGHT];
	
		for (int i=0 ; i < WIDTH*HEIGHT ; i++)
		{
			ilaArray[i] = %%RANDOM_VALUE%%;
		}
		%%NAME%%Ila ila = %%NAME%%IlaFromArray.create(ilaArray);
		
		%%TYPE%%[][] ilmArray = new %%TYPE%%[HEIGHT][WIDTH];
		
		System.arraycopy(ilaArray, WIDTH*0, ilmArray[0], 0, WIDTH);
		System.arraycopy(ilaArray, WIDTH*1, ilmArray[1], 0, WIDTH);
		System.arraycopy(ilaArray, WIDTH*2, ilmArray[2], 0, WIDTH);
		System.arraycopy(ilaArray, WIDTH*3, ilmArray[3], 0, WIDTH);
		System.arraycopy(ilaArray, WIDTH*4, ilmArray[4], 0, WIDTH);
		System.arraycopy(ilaArray, WIDTH*5, ilmArray[5], 0, WIDTH);
		System.arraycopy(ilaArray, WIDTH*6, ilmArray[6], 0, WIDTH);
		System.arraycopy(ilaArray, WIDTH*7, ilmArray[7], 0, WIDTH);
		System.arraycopy(ilaArray, WIDTH*8, ilmArray[8], 0, WIDTH);
		
		%%NAME%%Ilm ilm = %%NAME%%IlmFromArray.create(ilmArray);

		try
		{
			%%NAME%%IlmCheck.check(ilm,
				%%NAME%%IlmFrom%%NAME%%Ila.create(ila, WIDTH));
		}
		catch(IllegalArgumentException iae)
		{
			iae.printStackTrace();
			fail(iae.getMessage());
		}
	}
}
