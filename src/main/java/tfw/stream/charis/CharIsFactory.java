package tfw.stream.charis;

/**
 * This interface defines a factory that creates CharIs objects.
 */
public interface CharIsFactory {
	/**
	 * Create a CharIs object.
	 * 
	 * @return the CharIs object.
	 */
	CharIs create();
}
